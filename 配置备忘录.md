
# python加载

# 在 R Console 中执行
remove.packages(c("knitr", "reticulate"))
install.packages(c("knitr", "reticulate"))

# 然后测试
library(knitr)
library(reticulate)
knit_engines$set(python = reticulate::eng_python)
names(knit_engines$get())  # 应该包含 "python"


配置说明