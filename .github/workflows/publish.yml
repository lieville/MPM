on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '**.qmd'
      - '**.Rmd'
      - '**.md'
      - 'quarto.yml'
      - 'quarto.lock'
      - '_quarto.yml'
      - '_extensions/**'

name: Quarto Publish

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      R_LIBS_USER: /tmp/R/library
      PYTHON_PACKAGES: /tmp/python/packages
      
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      # 1. 安装系统依赖
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libpng-dev \
            libxml2-dev \
            libssl-dev \
            libcurl4-openssl-dev \
            python3-dev \
            python3-pip

      # 2. 安装Python并缓存
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache Python packages
        uses: actions/cache@v4
        id: cache-python
        with:
          path: |
            ~/.cache/pip
            ${{ env.PYTHON_PACKAGES }}
          key: ${{ runner.os }}-python-3.12-${{ hashFiles('**/requirements.txt', '**/pyproject.toml', '**/Pipfile', '**/Pipfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-python-3.12-

      - name: Install Python dependencies
        if: steps.cache-python.outputs.cache-hit != 'true'
        run: |
          pip install --upgrade pip
          pip install jupyter numpy pandas scikit-learn matplotlib seaborn
          # 创建requirements.txt以更好地管理依赖
          pip freeze > /tmp/requirements.txt

      # 3. 安装R并缓存
      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.5.2'

      - name: Cache R packages
        uses: actions/cache@v4
        id: cache-r
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-4.5.2-${{ hashFiles('DESCRIPTION', 'renv.lock') }}
          restore-keys: |
            ${{ runner.os }}-r-4.5.2-

      - name: Install R packages
        if: steps.cache-r.outputs.cache-hit != 'true'
        run: |
          mkdir -p ${{ env.R_LIBS_USER }}
          echo "R_LIBS_USER=${{ env.R_LIBS_USER }}" >> ~/.Renviron
          
          Rscript -e '
          options(repos = c(CRAN = "https://cloud.r-project.org/"))
          
          # 定义要安装的包
          packages <- c(
            "rmarkdown",
            "knitr",
            "reticulate",
            "downlit",
            "xml2"
          )
          
          # 安装缺失的包
          installed <- installed.packages()
          to_install <- packages[!packages %in% rownames(installed)]
          
          if (length(to_install) > 0) {
            install.packages(to_install, lib = Sys.getenv("R_LIBS_USER"))
          }
          
          # 验证安装
          for (pkg in packages) {
            if (!require(pkg, character.only = TRUE, quietly = TRUE)) {
              stop("Failed to load package: ", pkg)
            }
          }
          cat("All R packages installed and loaded successfully\n")
          '

      - name: Verify R and Python integration
        run: |
          Rscript -e '
          if (require("reticulate")) {
            cat("Reticulate version:", as.character(packageVersion("reticulate")), "\n")
            cat("Python found:", reticulate::py_available(), "\n")
            if (reticulate::py_available()) {
              cat("Python version:", reticulate::py_config()$version, "\n")
            }
          } else {
            stop("reticulate package not available")
          }
          '

      - name: Render and Publish
        uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}