on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '**.qmd'
      - '**.Rmd'
      - '**.md'
      - 'quarto.yml'
      - 'quarto.lock'
      - '_quarto.yml'
      - '_extensions/**'

name: Quarto Publish

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      R_LIBS_USER: /tmp/R/library
      
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          tinytex: true

      # 安装 Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 安装 Python 包（根据项目需要调整）
      - name: Install Python dependencies
        run: |
          pip install numpy pandas matplotlib seaborn scikit-learn

      # 安装系统依赖
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libpng-dev \
            libxml2-dev \
            libssl-dev \
            libcurl4-openssl-dev \
            libfontconfig1-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libfreetype6-dev \
            libtiff5-dev \
            libjpeg-dev

      # 安装中文字体
      - name: Install Chinese fonts
        run: |
          sudo apt-get install -y fonts-noto-cjk texlive-lang-chinese

      # 安装 R
      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.5.2'

      # 安装 R 包依赖（包含 reticulate 用于 Python 集成）
      - name: Install R packages
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::rmarkdown
            any::knitr
            any::downlit
            any::xml2
            any::ggplot2
            any::dplyr
            any::tidyverse
            any::reticulate
          extra-packages: |
            any::rcmdcheck
          needs: check

      # 配置 reticulate 使用系统 Python
      - name: Configure R reticulate
        run: |
          Rscript -e "reticulate::use_python('$(which python)', required = TRUE)"

      # 确保 gh-pages 分支存在
      - name: Ensure gh-pages branch exists
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          if git ls-remote --heads origin gh-pages | grep -q gh-pages; then
            echo "gh-pages branch already exists"
          else
            echo "Creating gh-pages branch..."
            git checkout --orphan gh-pages
            git reset --hard
            git commit --allow-empty -m "Initialize gh-pages branch"
            git push origin gh-pages
            git checkout main
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 渲染并发布
      - name: Render and Publish
        uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}