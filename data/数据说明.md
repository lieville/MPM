
数据集       	R 中位置	               Python 中位置	                  类型
鸢尾花	        iris (datasets)	sklearn.datasets.load_iris()	          分类
汽车数据	        mtcars (datasets)	无内置	                         回归
小费数据	         无内置	     seaborn.load_dataset('tips')	        回归
钻石数据	      diamonds (ggplot2)	seaborn.load_dataset('diamonds')	回归
泰坦尼克	       无内置	seaborn.load_dataset('titanic')	        分类
波士顿房价  	Boston (MASS)	sklearn.datasets.load_boston()	回归
葡萄酒	     wine (无内置)	sklearn.datasets.load_wine()	分类


# R和R包自带数据集
Ionosphere   包mlbench    分类任务
Sonar           包mlbench    分类任务


# R加载内置数据集
data(Ionosphere,package="mlbench") 
write.csv(Ionosphere, "Ionosphere.csv", row.names = FALSE)


# Python:加载内置数据集
from sklearn import datasets
iris = datasets.load_iris()


# R读写
df <- read.csv("data.csv", 
               header = TRUE,        # 有标题行
               stringsAsFactors = FALSE,  # 不将字符串转为因子
               na.strings = c("", "NA", "NULL"))

library(readxl)
df <- read_excel("data.xlsx", sheet = 1)


write.csv(df, "output.csv", 
          row.names = FALSE,        # 不写入行名
          na = "",                  # 空值处理
          fileEncoding = "UTF-8")

library(writexl)
write_xlsx(df, "output.xlsx")


# Python读写
import pandas as pd
df = pd.read_csv("data.csv",
                 header=0,           # 有标题行
                 na_values=["", "NA", "NULL"],
                 keep_default_na=True)

df = pd.read_excel("data.xlsx", 
                   sheet_name=0,    # 第一个工作表
                   engine='openpyxl')                 

df = pd.DataFrame(diabetes.data, columns=diabetes.feature_names)
df.to_csv("diabetes.csv",
          index=False,             # 不写入索引
          na_rep="",               # 空值替换
          encoding="utf-8",
          float_format="%.2f")     # 浮点数格式

df.to_excel("diabetes.xlsx",
            index=False,
            sheet_name="Sheet1")